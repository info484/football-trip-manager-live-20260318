<%- include('partials/header') %>

<section class="page-header">
  <h1>Dashboard</h1>
  <p>Overzicht van leden, wedstrijden en inschrijvingen.</p>
</section>

<% if (message) { %>
  <div class="alert <%= messageType === 'error' ? 'alert-error' : 'alert-success' %>"><%= message %></div>
<% } %>

<section class="stats-grid">
  <article class="card stat-card">
    <h2><%= stats.members %></h2>
    <p>Leden</p>
  </article>
  <article class="card stat-card">
    <h2><%= stats.openMatches %></h2>
    <p>Open wedstrijden</p>
  </article>
  <article class="card stat-card">
    <h2><%= stats.registrations %></h2>
    <p>Inschrijvingen</p>
  </article>
</section>

<section class="card">
  <div class="card-header">
    <h2>Aankomende wedstrijden</h2>
    <% if (isDeveloper) { %>
      <a class="btn" href="/matches/new">Nieuwe wedstrijd</a>
    <% } %>
  </div>

  <div class="table-wrap">
    <table>
      <thead>
        <tr>
          <th>Datum</th>
          <th>Tegenstander</th>
          <th>Locatie</th>
          <th>Punten</th>
          <th>Aanwezig</th>
        </tr>
      </thead>
      <tbody>
        <% if (upcomingMatches.length === 0) { %>
          <tr>
            <td colspan="5">Nog geen wedstrijden.</td>
          </tr>
        <% } %>
        <% upcomingMatches.forEach((match) => { %>
          <tr>
            <td><a href="/matches/<%= match.id %>"><%= new Date(match.match_datetime).toLocaleString('nl-BE') %></a></td>
            <td><%= match.opponent %></td>
            <td><%= match.location %></td>
            <td><%= match.points_value %></td>
            <td><%= match.present_count %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</section>

<section class="card">
  <h2>Top 10 loyalty leden</h2>
  <div class="table-wrap">
    <table>
      <thead>
        <tr>
          <th>Positie</th>
          <th>Lid</th>
          <th>Punten</th>
        </tr>
      </thead>
      <tbody>
        <% if (topLoyalty.length === 0) { %>
          <tr>
            <td colspan="3">Nog geen loyalty data.</td>
          </tr>
        <% } %>
        <% topLoyalty.forEach((member, index) => { %>
          <tr>
            <td><strong>#<%= index + 1 %></strong></td>
            <td><%= member.first_name %> <%= member.last_name %></td>
            <td><%= member.total_points %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</section>

<%- include('partials/footer') %>
